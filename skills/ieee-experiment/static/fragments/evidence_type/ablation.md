# Ablation Evidence

Each claimed module needs an ablation:

1. remove the module,
2. replace it with a simple alternative,
3. vary key hyperparameters,
4. measure the claimed benefit.

Weak ablation only removes components without explaining what the result means.
