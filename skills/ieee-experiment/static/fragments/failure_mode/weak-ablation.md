# Weak Ablation

An ablation is weak when it:

1. removes modules but does not map to claims,
2. lacks a simple replacement baseline,
3. reports only one metric,
4. ignores the condition that motivated the module.

Fix by tying each ablation row to a contribution statement.
