# Dataset or Code

Expected metadata:

dataset/code title, authors or organization, repository, version, year/date, DOI or stable URL, access date when required.

Use dataset citations when experiments depend on public resources.
