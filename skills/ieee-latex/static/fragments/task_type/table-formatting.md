# Table Formatting

Prefer readable tables over over-compressed tables.

Use consistent precision, units in headers, clear method groups, and column widths that survive IEEE column scaling.

If a table is too wide, consider splitting, rotating only if allowed, reducing columns, or using a two-column table.
