# Float Placement

Use `figure`/`table` for one-column floats and `figure*`/`table*` for necessary two-column floats.

Avoid excessive `[h]` forcing. Place floats near first citation and keep captions with objects.
