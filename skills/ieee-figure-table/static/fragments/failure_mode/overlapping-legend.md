# Overlapping Legend

Legends should not cover data or force the reader to decode tiny colors.

Fix:

1. move legend outside the data area,
2. use direct labels,
3. reduce series count,
4. split into panels,
5. use line styles and markers.
