# Missing Classic Work

If a field has expected classical baselines or foundational methods, flag their absence when visible from context.

Do not invent exact references; state the type of missing work if not known.
