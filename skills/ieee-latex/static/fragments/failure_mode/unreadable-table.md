# Unreadable Table

Fix structure first:

1. reduce columns,
2. split table,
3. group metrics,
4. use meaningful abbreviations,
5. use two-column width if justified.

Font shrinking is a last resort.
