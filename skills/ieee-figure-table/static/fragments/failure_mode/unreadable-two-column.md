# Unreadable Two-Column

If a figure is unreadable in one column:

1. simplify labels,
2. increase font and line sizes,
3. split into panels,
4. use `figure*` only when the evidence needs full width,
5. redesign the figure rather than shrinking it.

Always judge readability at final PDF scale.
